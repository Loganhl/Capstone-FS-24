import React from "react";
import { Nav,Navbar } from "react-bootstrap";

const Footer = ({token,client})=>{
    return(
        <Navbar variant="dark">
            
        </Navbar>
    )
}

export default Footer;